---
import Container from './common/Container';
import Button from './common/Button';

// Define os itens do menu
const menuItems = [
  { href: '/', text: 'Home' },
  { href: '/servicos', text: 'Serviços' },
  { href: '/portfolio', text: 'Portfolio' },
  // { href: '/sobre', text: 'Sobre' }, // Mantido comentado
  { href: '/contato', text: 'Contato' }
];

// Acessa o pathname da URL atual corretamente
const currentPath = Astro.request.url.pathname;
---
<header class="bg-dark/80 backdrop-blur-md fixed w-full z-50 border-b border-gray-700/50">
  <Container>
    <div class="py-4 flex justify-between items-center">
      <a href="/" class="text-2xl font-bold text-white flex items-center gap-2">
        <img src="/images/Branco fundo transparente.png" alt="LYTSPOT" class="h-16" />
      </a>

      <nav class="hidden md:flex space-x-6">
        {menuItems.map(item => (
          <a 
            href={item.href}
            class={`text-gray-200 hover:text-white transition-colors ${item.href === currentPath ? 'text-white' : ''}`}
          >
            {item.text}
          </a>
        ))}
      </nav>

      <Button variant="primary" href="/contato">
        Orçamento
      </Button>
    </div>
  </Container>
</header>
