---
import Container from './common/Container';
import Button from './common/Button';
---

<section class="relative h-screen flex items-center justify-center">
  <div class="absolute inset-0 z-0">
    <video 
      autoplay 
      loop 
      muted 
      playsinline 
      class="w-full h-full object-cover"
    >
      <source src="/videos/drone-hero.mp4" type="video/mp4">
    </video>
    <div class="absolute inset-0 bg-dark/70"></div>
  </div>

  <Container>
    <div class="relative z-10 text-center text-white">
      <h1 class="text-5xl md:text-7xl font-bold mb-6">
        Capturando momentos únicos
      </h1>
      <p class="text-xl md:text-2xl mb-8 max-w-2xl mx-auto text-gray-100">
        Especialistas em filmagens aéreas e fotografia profissional para seus eventos e projetos
      </p>
      <div class="flex gap-4 justify-center">
        <Button variant="primary" size="lg" client:load>
          Ver Portfólio
          <Button
          variant="outline"
          size="lg"
          client:load
          style="background-color: white; color: #042414; border: 1px solid #042414;"
        >
          Fale Conosco
        </Button>
        </Button>
      </div>
    </div>
  </Container>
</section>